// 1\ 定义正则
const tagName = //;

// 2. 指针移动

// 3. 循环解析html （进行分组）
    // 1、匹配标签
        // 1、匹配起始标签 parseStartTag
        // 2、匹配结束标签 parseEndTag
    // 2、匹配文本 parseText
    // 不考虑注释节点

// 正则匹配一般是1、是先用正则判断是否
export const parse = template => {
    let stack = [];
    let root;
    let currentParent;
};
